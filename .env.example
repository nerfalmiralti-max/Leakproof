# Explicit mode selection. No automatic fallback from real analysis to demo.
ANALYSIS_PROVIDER=demo
# Required on the SERVER only when ANALYSIS_PROVIDER=openai.
OPENAI_API_KEY=
OPENAI_VISION_MODEL=gpt-4.1-mini
